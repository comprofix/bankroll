<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
      Cleartext (plain HTTP) is permitted ONLY to these specific local-testing
      addresses, not globally. This is deliberate: the server URL is
      user-entered at runtime (see capacitor-www/index.html), so a blanket
      cleartext allowance would mean anyone who typed http:// instead of
      https:// for a real deployment would silently have their session
      cookie and data sent in plaintext. Real deployments must use HTTPS.

      10.0.2.2 is the Android emulator's fixed alias for its host machine's
      localhost, the standard way to reach a locally-running dev server from
      inside the emulator. Add your own machine's LAN IP here too if testing
      from a physical device on the same network.
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
